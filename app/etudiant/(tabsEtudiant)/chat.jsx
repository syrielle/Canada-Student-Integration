import { Text, View } from 'react-native';

export default function MonChat() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Chat (à implémenter)</Text>
    </View>
  );
}
